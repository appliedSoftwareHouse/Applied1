@page
@using System.Data
@model Applied_WebApplication.Pages.Accounts.COA_AddModel
@{
}

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
</head>

<div class="mt-3 p-2 bg-success text-white rounded">
    <h3>CHART OF ACCOUNTS - ADD </h3>
</div>
<br />

<form method="post">


    <div class="container text-md-start w3-border-blue-grey g-1">
        <div class="text-danger" asp-validation-summary="ModelOnly"></div>
        <div class="row text-start">
            <label class="col-sm-2 col-form-label-sm" asp-for="_Record.ID">Account ID</label>
            <div class="col-sm-10">
                <input type="text" style="width:200px" class="form-control form-control-sm" id="txtID" name="ID" asp-for="_Record.ID">
            </div>
        </div>
        <div class="row text-start">
            <label class="col-sm-2 col-form-label-sm" asp-for="_Record.Code">Account Code</label>
            <div class="col-sm-10">
                <input type="text" style="width:200px" class="form-control form-control-sm" id="txtCode" name="Code" asp-for="_Record.Code">
            </div>
        </div>
        <div class="row text-start">
            <label class="col-sm-2 col-form-label-sm" asp-for="_Record.Title">Account Title</label>
            <div class="col-sm-10">
                <input type="text" style="width:400px" class="form-control form-control-sm" id="txtTitle" name="Title" asp-for="_Record.Title">
            </div>
        </div>
        <div class="row text-start">
            <label class="col-sm-2 col-form-label-sm" asp-for="_Record.OBal">Opening Balance</label>
            <div class="col-sm-10">
                <input type="text" style="width:200px" class="form-control form-control-sm" id="txtOBal" name="Opening Balance" asp-for="_Record.OBal">
            </div>
        </div>
    </div>

    <div class="container-lg pt-5">
        <div class="Row">
        </div>
        <br />
        <div class="row-cols-3">
            <div class="input-group mb-3">
                <label class="input-group-text g-2" for="hd_Class" disabled style="width:250px;">Class of Account</label>
                <select class="form-select" id="inputCOA_Class" name="COA_Class" asp-for=_Record.COA_Class>
                    <option selected>@Model.Title_Class</option>
                    @if (Model.COA_Class.MyDataTable.Rows != null)
                    {
                        @foreach (DataRow _Record in Model.COA_Class.MyDataTable.Rows)
                        {
                            <option value=@_Record["ID"]> @_Record["Title"] </option>
                        }
                    }
                </select>
            </div>
            <div class="input-group mb-3">
                <label class="input-group-text g-2" for="hd_Nature" disabled style="width:250px;">Nature of Account</label>
                <select class="form-select" id="inputCOA_Nature" name="COA_Nature" asp-for=_Record.COA_Nature>
                    <option selected>@Model.Title_Nature</option>
                    @if (Model.COA_Nature.MyDataTable.Rows != null)
                    {
                        @foreach (DataRow _Record in Model.COA_Nature.MyDataTable.Rows)
                        {
                            <option value=@_Record["ID"]> @_Record["Title"] </option>
                        }
                    }
                </select>
            </div>
            <div class="input-group mb-3">
                <label class="input-group-text g-2" for="hd_Nature" disabled style="width:250px;">Notes of Account</label>
                <select class="form-select" id="inputCOA_Notes" name="COA_Notes" asp-for=_Record.COA_Notes>
                    <option selected>@Model.Title_Notes</option>
                    @if (Model.COA_Notes.MyDataTable.Rows != null)
                    {
                        @foreach (DataRow _Record in Model.COA_Notes.MyDataTable.Rows)
                        {
                            <option value=@_Record["ID"]> @_Record["Title"] </option>
                        }
                    }
                </select>
            </div>
        </div>
        <br />

        <div class="row" style="width:400px;">

            <div class="form-group row" style="width:50%">
                <div class="col-5">  <input type="submit" class="button btn-primary" value="Save" asp-page-handler="Submit" asp-route-UserName=@User.Identity.Name />   </div>
            </div>

            <div class="form-group row" style="width:50%">
                <div class="col-5">  <input type="submit" class="button btn-primary" value="Back" asp-page-handler="Back" />   </div>
            </div>
        </div>
    </div>

    <br />

    <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModal">
        Launch demo modal
    </button>

    <!-- Modal -->
    <div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLabel">Modal title</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    ...
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                    <button type="button" class="btn btn-primary">Save changes</button>
                </div>
            </div>
        </div>
    </div>


</form>