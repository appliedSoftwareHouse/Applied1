@page
@using System.Data;
@model Applied_WebApplication.Pages.Sales.CreateInvoiceModel
@{
    var UserName = User.Identity.Name;
    var NumberFormat = AppRegistry.GetText(UserName, "FMTCurrency");



}


<div class="container">
    <div class="row">
        <div class="col-3">
            <
            

        </div>
    </div>
</div>

<div class="container">

    @if (Model.ExpenseSheet != null)
    {
        <table class="table-list">
            <thead>
                <tr>
                    <th>Heading1</th>
                    <th></th>
                    <th></th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (DataRow Row in Model.Tb_Expense.Rows)
                {
                    decimal _DR = (decimal)Row["DR"];
                    decimal _CR = (decimal)Row["CR"];
                    string  _Amount = (_DR - _CR).ToString(NumberFormat);

                    <tr>
                        <td>@Row["Vou_No"]</td>
                        <td>@Row["AccountTitle"]</td>
                        <td>
                            <div class="input-group input-group-sm mb-3">
                                <select class="form-select" id="inputGroupSelect02">
                                    <option selected>Choose...</option>
                                    <option value="1">One</option>
                                    <option value="2">Two</option>
                                    <option value="3">Three</option>
                                </select>
                                @* <label class="input-group-text" for="inputGroupSelect02">Options</label> *@
                            </div>
                        </td>
                        <td></td>
                        <td class="amount">@_Amount</td>
                    </tr>
                }
            </tbody>
            <tfoot>
                <tr>
                    <td>Footer</td>
                    <td></td>
                    <td></td>
                    <td></td>
                    <td></td>
                </tr>
            </tfoot>
        </table>
    }
</div>